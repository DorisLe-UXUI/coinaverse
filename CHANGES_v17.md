# Coinaverse v16 → v17 — May 30, 2026 · REAL 3D ART WIRED IN
Assets from Coincept_uiux.zip (Doris's MidJourney gen) integrated.

## Wired this pass (the big 3)
1. **6 hub MAP arts** → assets/map/<token>.jpeg. hubRadialMap now renders the real
   floating-island radial map per hub (replaced CSS-art radial). CredTech keeps credtech_central.
2. **6 hero characters** → split into:
   - portrait (1:1) → pills/cards/CUSTOMIZE mini
   - hero (full body) → home walking char, hub info figure, world_detail hero, avatar studio
3. **48 district tiles** → 3D thumbnail on every hub-games card (8/hub × 6 hubs).

All auto-derived from archetype token, themed by the locked signature colors.

## Already in hand (gen'd, NOT yet wired — next pass v18)
- Screen bgs: welcome / ai_welcome / game-over / party lobby / leaderboard podium / level-up / loading
- COINAVERSE_CENTRAL labeled map (6 islands) — can replace home bg if hotspots re-tuned
- Currency tokens (Coinaverse coin + hub tokens + cred gem), UI icons, FX, rank badges, chest, logos
- Hero victory + walk poses (for reward / game sprite)

## Still genuinely missing
- CredTech 8 district tiles + hub content → blocked on full CredTech flow (PDF was cut off)
- Decisions: Builder Purple/Yellow · Rebuilder color · CredTech archetype
- Optional: avatar gear/cosmetic items · cinema/shop reskin · multi-character hub rosters
